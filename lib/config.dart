/// 配置
class CatWifiConfig {
  /// app 持久化存储的 `key`
  static final String fsApp = "catwifi_app";

  /// 初次运行的说明
  static final String firstRunKey = "first_run";

  /// wifi 列表
  static final String wifiListKey = "wifi_list";

  /// 初次运行说明
  static final String firstRunHelp =
      "这款工具的用处就是可以解析别人分享给你的二维码 `wifi` 里的数据, 让你不再尴尬的去问`朋友`/`同学`/`家人`/`表面兄弟`的`wifi`密码, 据小爷所知, 华为和小米都支持";
}
