import 'package:catwifi/app.dart';
import 'package:flutter/material.dart';

void main() => runApp(CatWifiAPP());

class CatWifiAPP extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: App(),
    );
  }
}
